# URL base da API do backend
VITE_API_URL=http://localhost:4000

# Token da equipe para a tela de leitura do tablet (deve ser igual ao STAFF_TOKEN do backend)
# Obs: fica exposto no build do frontend. Use uma chave dedicada apenas para o evento.
VITE_STAFF_TOKEN=troque-esta-chave
